filter {
  if "bro_smbfiles" in [tags] {
    csv {
      separator => "	"

      # #fields ts  uid id.orig_h   id.orig_p   id.resp_h   id.resp_p   fuid    action  path    name    size    prev_name   times.modified  times.accessed  times.created   times.changed
      columns => [ "timestamp", "uid", "source_ip", "source_port", "destination_ip", "destination_port", "file_uid", "file_action", "file_path", "file_name", "file_size", "file_previousname", "times.modified", "times.accessed", "times.created", "times.changed" ]
    }    
  }  
}
