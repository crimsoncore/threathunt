filter {
  if [log_name] == "Security" {
    mutate {
        remove_tag => ["beats_input_codec_plain_applied"]
        add_tag => [ "security" ]
        add_field => { "LogType" => "windows-security" }
    }
  }  
}
