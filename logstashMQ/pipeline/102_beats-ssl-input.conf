input {
  beats {
    port => 5045
    type => "beats"
    ssl => "true"
#    ssl_certificate_authorities => "/usr/share/logstash/pipeline/datalake_ca_0.crt"
    ssl_certificate => "/usr/share/logstash/pipeline/server.crt"
    ssl_key => "/usr/share/logstash/pipeline/server.key"
  }
}
