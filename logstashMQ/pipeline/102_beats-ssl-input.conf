input {
  beats {
    port => 5045
    type => "beats"
    ssl => "true"
    ssl_certificate_authorities => "/usr/share/logstash/certs/datalake_ca_0.crt"
    ssl_certificate => "/usr/share/logstash/certs/server.crt"
    ssl_key => "/usr/share/logstash/certs/server.key"
  }
}
